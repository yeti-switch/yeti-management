daemon {
        listen = {
                "tcp://127.0.0.1:4444"
        }
        sctp {
            address = "127.0.0.1"
            port = 4444
        }
        http {
            address = "127.0.0.1"
            port = 3000
        }
        log_level = 2
}
